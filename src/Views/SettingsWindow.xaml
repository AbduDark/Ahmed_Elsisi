
<Window x:Class="LineManagementSystem.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="⚙️ الإعدادات" 
        Height="500" Width="600"
        WindowStartupLocation="CenterScreen"
        FlowDirection="RightToLeft"
        Background="{DynamicResource WindowBackground}"
        ResizeMode="NoResize">

    <Window.Resources>
        <Style x:Key="SectionCard" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource CardBackground}"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" Opacity="0.08" BlurRadius="10" ShadowDepth="2"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SectionTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>

        <Style x:Key="ToggleSwitch" TargetType="CheckBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="Margin" Value="0,8"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Style="{StaticResource SectionCard}">
            <StackPanel>
                <TextBlock Text="⚙️ إعدادات التطبيق" 
                          FontSize="24" 
                          FontWeight="Bold"
                          Foreground="{DynamicResource TextPrimary}"
                          HorizontalAlignment="Center"/>
                <TextBlock Text="برنامج احمد السيسي لإدارة الموبايل" 
                          FontSize="14" 
                          Foreground="{DynamicResource TextSecondary}"
                          HorizontalAlignment="Center"
                          Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Settings Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Theme Settings -->
                <Border Style="{StaticResource SectionCard}">
                    <StackPanel>
                        <TextBlock Style="{StaticResource SectionTitle}">
                            <Run Text="🎨"/>
                            <Run Text=" المظهر والألوان"/>
                        </TextBlock>

                        <DockPanel Margin="0,10">
                            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                                <TextBlock Text="🌙" FontSize="24" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="الوضع الليلي" 
                                             FontSize="16" 
                                             FontWeight="SemiBold"
                                             Foreground="{DynamicResource TextPrimary}"/>
                                    <TextBlock Text="تفعيل الوضع الداكن لراحة العين" 
                                             FontSize="12" 
                                             Foreground="{DynamicResource TextSecondary}"/>
                                </StackPanel>
                            </StackPanel>
                            
                            <CheckBox x:Name="DarkModeToggle"
                                    DockPanel.Dock="Left"
                                    IsChecked="{Binding IsDarkMode}"
                                    Checked="DarkModeToggle_Changed"
                                    Unchecked="DarkModeToggle_Changed"
                                    VerticalAlignment="Center"
                                    Margin="10,0,0,0"/>
                        </DockPanel>

                        <Border Height="1" Background="{DynamicResource BorderColor}" Margin="0,15"/>

                        <TextBlock x:Name="CurrentThemeText"
                                 FontSize="14"
                                 Foreground="{DynamicResource TextSecondary}"
                                 Margin="0,10,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Application Info -->
                <Border Style="{StaticResource SectionCard}">
                    <StackPanel>
                        <TextBlock Style="{StaticResource SectionTitle}">
                            <Run Text="ℹ️"/>
                            <Run Text=" معلومات التطبيق"/>
                        </TextBlock>

                        <StackPanel Margin="0,10">
                            <DockPanel Margin="0,8">
                                <TextBlock Text="الإصدار:" 
                                         FontWeight="SemiBold"
                                         Foreground="{DynamicResource TextPrimary}"
                                         DockPanel.Dock="Right"/>
                                <TextBlock Text="1.0.0" 
                                         Foreground="{DynamicResource TextSecondary}"
                                         DockPanel.Dock="Left"
                                         Margin="10,0,0,0"/>
                            </DockPanel>

                            <DockPanel Margin="0,8">
                                <TextBlock Text="المطور:" 
                                         FontWeight="SemiBold"
                                         Foreground="{DynamicResource TextPrimary}"
                                         DockPanel.Dock="Right"/>
                                <TextBlock Text="احمد السيسي" 
                                         Foreground="{DynamicResource TextSecondary}"
                                         DockPanel.Dock="Left"
                                         Margin="10,0,0,0"/>
                            </DockPanel>

                            <DockPanel Margin="0,8">
                                <TextBlock Text="قاعدة البيانات:" 
                                         FontWeight="SemiBold"
                                         Foreground="{DynamicResource TextPrimary}"
                                         DockPanel.Dock="Right"/>
                                <TextBlock Text="SQLite" 
                                         Foreground="{DynamicResource TextSecondary}"
                                         DockPanel.Dock="Left"
                                         Margin="10,0,0,0"/>
                            </DockPanel>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Future Settings Placeholder -->
                <Border Style="{StaticResource SectionCard}">
                    <StackPanel>
                        <TextBlock Style="{StaticResource SectionTitle}">
                            <Run Text="🚀"/>
                            <Run Text=" ميزات قادمة"/>
                        </TextBlock>

                        <TextBlock Text="• إعدادات التنبيهات والإشعارات" 
                                 FontSize="14"
                                 Foreground="{DynamicResource TextSecondary}"
                                 Margin="0,5"/>
                        <TextBlock Text="• خيارات النسخ الاحتياطي والاستعادة" 
                                 FontSize="14"
                                 Foreground="{DynamicResource TextSecondary}"
                                 Margin="0,5"/>
                        <TextBlock Text="• إعدادات الباركود والماسح الضوئي" 
                                 FontSize="14"
                                 Foreground="{DynamicResource TextSecondary}"
                                 Margin="0,5"/>
                        <TextBlock Text="• تخصيص التقارير والتصدير" 
                                 FontSize="14"
                                 Foreground="{DynamicResource TextSecondary}"
                                 Margin="0,5"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Footer Buttons -->
        <Border Grid.Row="2" Margin="0,15,0,0">
            <Button Content="✓ إغلاق" 
                    Click="CloseButton_Click"
                    Padding="30,12"
                    Background="#4CAF50"
                    Foreground="White"
                    FontSize="16"
                    FontWeight="Bold"
                    BorderThickness="0"
                    Cursor="Hand"
                    HorizontalAlignment="Center">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="10"
                                Padding="{TemplateBinding Padding}">
                            <Border.Effect>
                                <DropShadowEffect Color="Black" Opacity="0.2" BlurRadius="10" ShadowDepth="3"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Border>
    </Grid>
</Window>
