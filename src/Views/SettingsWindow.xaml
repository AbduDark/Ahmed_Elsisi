<Window x:Class="LineManagementSystem.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="⚙️ الإعدادات" 
        Height="700" Width="900"
        WindowStartupLocation="CenterScreen"
        FlowDirection="RightToLeft"
        Background="{DynamicResource WindowBackground}"
        ResizeMode="CanResize">

    <Window.Resources>
        <Style x:Key="SectionCard" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource CardBackground}"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" Opacity="0.08" BlurRadius="10" ShadowDepth="2"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SectionTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>

        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <Border.Effect>
                                <DropShadowEffect Color="Black" Opacity="0.15" BlurRadius="8" ShadowDepth="2"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource SectionCard}">
            <StackPanel>
                <TextBlock Text="⚙️ إعدادات التطبيق" 
                          FontSize="24" 
                          FontWeight="Bold"
                          Foreground="{DynamicResource TextPrimary}"
                          HorizontalAlignment="Center"/>
                <TextBlock Text="نظام إدارة خطوط الهاتف المحمول" 
                          FontSize="14" 
                          Foreground="{DynamicResource TextSecondary}"
                          HorizontalAlignment="Center"
                          Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <Border Style="{StaticResource SectionCard}">
                    <StackPanel>
                        <TextBlock Style="{StaticResource SectionTitle}">
                            <Run Text="💾"/>
                            <Run Text=" النسخ الاحتياطي والاستعادة"/>
                        </TextBlock>

                        <WrapPanel Orientation="Horizontal" Margin="0,0,0,15">
                            <Button Content="📥 إنشاء نسخة احتياطية" 
                                    Style="{StaticResource ActionButton}"
                                    Background="#4CAF50"
                                    Click="CreateBackup_Click"/>
                            <Button Content="📤 استعادة من نسخة" 
                                    Style="{StaticResource ActionButton}"
                                    Background="#2196F3"
                                    Click="RestoreBackup_Click"/>
                            <Button Content="📂 فتح مجلد النسخ" 
                                    Style="{StaticResource ActionButton}"
                                    Background="#FF9800"
                                    Click="OpenBackupFolder_Click"/>
                            <Button Content="🗑️ حذف نسخ قديمة" 
                                    Style="{StaticResource ActionButton}"
                                    Background="#F44336"
                                    Click="DeleteOldBackups_Click"/>
                        </WrapPanel>

                        <Border Height="1" Background="{DynamicResource BorderColor}" Margin="0,10"/>

                        <DockPanel Margin="0,15">
                            <StackPanel DockPanel.Dock="Right">
                                <TextBlock Text="النسخ الاحتياطي التلقائي" 
                                         FontSize="16" 
                                         FontWeight="SemiBold"
                                         Foreground="{DynamicResource TextPrimary}"/>
                                <TextBlock Text="إنشاء نسخة احتياطية تلقائياً كل 24 ساعة" 
                                         FontSize="12" 
                                         Foreground="{DynamicResource TextSecondary}"/>
                            </StackPanel>
                            
                            <CheckBox x:Name="AutoBackupToggle"
                                    DockPanel.Dock="Left"
                                    Checked="AutoBackup_Changed"
                                    Unchecked="AutoBackup_Changed"
                                    VerticalAlignment="Center"
                                    Margin="10,0,0,0"/>
                        </DockPanel>

                        <TextBlock x:Name="BackupStatusText"
                                 FontSize="13"
                                 Foreground="#4CAF50"
                                 FontWeight="SemiBold"
                                 Margin="0,10,0,0"/>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource SectionCard}">
                    <StackPanel>
                        <TextBlock Style="{StaticResource SectionTitle}">
                            <Run Text="📊"/>
                            <Run Text=" التقارير والتصدير"/>
                        </TextBlock>

                        <WrapPanel Orientation="Horizontal" Margin="0,0,0,10">
                            <Button Content="📄 تقرير شامل (Excel)" 
                                    Style="{StaticResource ActionButton}"
                                    Background="#43A047"
                                    Click="ExportExcel_Click"/>
                            <Button Content="📑 تقرير PDF" 
                                    Style="{StaticResource ActionButton}"
                                    Background="#E91E63"
                                    Click="ExportPDF_Click"/>
                            <Button Content="📈 إحصائيات الشركات" 
                                    Style="{StaticResource ActionButton}"
                                    Background="#9C27B0"
                                    Click="ProvidersStats_Click"/>
                        </WrapPanel>

                        <TextBlock Text="التقارير تشمل: قوائم الخطوط، المجموعات، التنبيهات، والإحصائيات الكاملة" 
                                 FontSize="12"
                                 Foreground="{DynamicResource TextSecondary}"
                                 Margin="0,10,0,0"
                                 TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource SectionCard}">
                    <StackPanel>
                        <TextBlock Style="{StaticResource SectionTitle}">
                            <Run Text="🎨"/>
                            <Run Text=" المظهر والألوان"/>
                        </TextBlock>

                        <DockPanel Margin="0,10">
                            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                                <TextBlock Text="🌙" FontSize="24" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="الوضع الليلي" 
                                             FontSize="16" 
                                             FontWeight="SemiBold"
                                             Foreground="{DynamicResource TextPrimary}"/>
                                    <TextBlock Text="تفعيل الوضع الداكن لراحة العين" 
                                             FontSize="12" 
                                             Foreground="{DynamicResource TextSecondary}"/>
                                </StackPanel>
                            </StackPanel>
                            
                            <CheckBox x:Name="DarkModeToggle"
                                    DockPanel.Dock="Left"
                                    IsChecked="{Binding IsDarkMode}"
                                    Checked="DarkModeToggle_Changed"
                                    Unchecked="DarkModeToggle_Changed"
                                    VerticalAlignment="Center"
                                    Margin="10,0,0,0"/>
                        </DockPanel>

                        <TextBlock x:Name="CurrentThemeText"
                                 FontSize="14"
                                 Foreground="{DynamicResource TextSecondary}"
                                 Margin="0,10,0,0"/>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource SectionCard}">
                    <StackPanel>
                        <TextBlock Style="{StaticResource SectionTitle}">
                            <Run Text="ℹ️"/>
                            <Run Text=" معلومات التطبيق"/>
                        </TextBlock>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                <DockPanel Margin="0,8">
                                    <TextBlock Text="المالك:" 
                                             FontWeight="SemiBold"
                                             Foreground="{DynamicResource TextPrimary}"
                                             DockPanel.Dock="Right"/>
                                    <TextBlock Text="أحمد السيسي" 
                                             Foreground="#4CAF50"
                                             FontWeight="SemiBold"
                                             DockPanel.Dock="Left"
                                             Margin="10,0,0,0"/>
                                </DockPanel>

                                <DockPanel Margin="0,8">
                                    <TextBlock Text="المطور:" 
                                             FontWeight="SemiBold"
                                             Foreground="{DynamicResource TextPrimary}"
                                             DockPanel.Dock="Right"/>
                                    <TextBlock Text="عبد الرحمن سيد" 
                                             Foreground="#2196F3"
                                             FontWeight="SemiBold"
                                             DockPanel.Dock="Left"
                                             Margin="10,0,0,0"/>
                                </DockPanel>
                            </StackPanel>

                            <StackPanel Grid.Column="1">
                                <DockPanel Margin="0,8">
                                    <TextBlock Text="الإصدار:" 
                                             FontWeight="SemiBold"
                                             Foreground="{DynamicResource TextPrimary}"
                                             DockPanel.Dock="Right"/>
                                    <TextBlock Text="2.0.0" 
                                             Foreground="{DynamicResource TextSecondary}"
                                             DockPanel.Dock="Left"
                                             Margin="10,0,0,0"/>
                                </DockPanel>

                                <DockPanel Margin="0,8">
                                    <TextBlock Text="قاعدة البيانات:" 
                                             FontWeight="SemiBold"
                                             Foreground="{DynamicResource TextPrimary}"
                                             DockPanel.Dock="Right"/>
                                    <TextBlock Text="SQLite" 
                                             Foreground="{DynamicResource TextSecondary}"
                                             DockPanel.Dock="Left"
                                             Margin="10,0,0,0"/>
                                </DockPanel>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <Border Grid.Row="2" Margin="0,15,0,0">
            <Button Content="✓ إغلاق" 
                    Click="CloseButton_Click"
                    Padding="40,12"
                    Background="#4CAF50"
                    Foreground="White"
                    FontSize="16"
                    FontWeight="Bold"
                    BorderThickness="0"
                    Cursor="Hand"
                    HorizontalAlignment="Center">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="10"
                                Padding="{TemplateBinding Padding}">
                            <Border.Effect>
                                <DropShadowEffect Color="Black" Opacity="0.2" BlurRadius="10" ShadowDepth="3"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Border>
    </Grid>
</Window>
